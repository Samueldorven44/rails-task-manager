<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= link_to "Edit une task", edit_task_path(@task) %>
<%= link_to "⬅ Retour à la liste", tasks_path %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
